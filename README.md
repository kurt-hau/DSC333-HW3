# [Class Code] — [Assignment Name]

**Course:** [e.g. DSC365 – Data Visualization]  
**Assignment:** [e.g. Homework 1]  
**Due Date:** [MM/DD/YYYY]

---

## Overview
[1–3 sentences describing what this assignment is about and what you built/analyzed.]

## Repo Structure

```
.
├── data/
│   ├── raw/          # Original source data (not committed to Git)
│   └── cleaned/      # Processed data generated by your code (not committed)
├── notebooks/        # R Markdown / Jupyter notebooks
├── figures/          # Charts, plots, screenshots of your visualizations
├── instructions/     # Assignment PDFs and reference materials
├── src/              # Reusable functions/modules
├── scripts/          # Standalone runnable scripts
├── reports/          # Final submission files (Word docs, PDFs)
└── env/              # Dependency files (requirements.txt, renv.lock)
```

## Setup

### R (RStudio)
```r
install.packages("renv")
renv::restore()
```

### Python
```bash
python -m venv .venv
source .venv/bin/activate      # Mac/Linux
# .venv\Scripts\activate       # Windows
pip install -r env/requirements.txt
```

## Notes
[Any assignment-specific notes, data sources, or known issues.]
